Instructions

1) open terminal and change directory to where the BST.py is located
2) Python3.4 BST.py X   # where X is the maximum number, e.g. if X = 55, 1-55 will be randomly inserted 						#into the tree

3) To view the a-c.dot files, you can convert them using "dot -Tpdf -o output.pdf input.dot"
or by installing Graphviz